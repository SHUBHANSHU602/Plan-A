# Data

This directory documents dataset schemas and reproducible acquisition steps.
Large, derived, licensed, or sensitive datasets must not be committed to Git.

- `raw/` is for immutable local source data.
- `processed/` is for reproducible derived datasets.
