# Machine learning

Training, evaluation, model packaging, and explainability code belongs here.

The backend integration contract is intentionally model-agnostic: inference must
return a probability, a predicted class, and zero or more human-readable drivers.
